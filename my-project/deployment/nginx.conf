server {
    listen 80;

    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Frontend Vue
    location /B3dev-TP_VUE/ {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}